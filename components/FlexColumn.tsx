import { Column as _Column } from 'carbon-components-react';
import styled from 'styled-components';

const FlexColumn = styled(_Column)`
  display: flex;
  align-items: center;
`;

export default FlexColumn;
